# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
test-results/
playwright-report/

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — commit .env.example only
.env*
!.env.example

# Playwright evidence (see docs/OPERATIONS.md § temp/)
/temp/*
!/temp/.gitkeep

# agent scratch
.superpowers/
.playwright-mcp/

# typescript
*.tsbuildinfo
next-env.d.ts

# Internal AI working files (kept locally, not part of the open-source repo)
CLAUDE.md
AGENTS.md
MEMORY.md
docs/AI_RULES.md
docs/PRD.md
docs/PLAN.md
docs/OPERATIONS.md
.claude/
skills-lock.json

# Historical borders (GPL-3.0 dataset) are fetched at build time by `pnpm data:borders`, never redistributed here
public/data/borders/

# Agent-workflow files: kept locally for Claude Code sessions, not part of the open-source repo
/CLAUDE.md
/AGENTS.md
/MEMORY.md
/docs/*.md
/.claude/
/skills-lock.json
.vercel

# local Python virtualenvs (tooling, never part of the app)
.venv*/

# Files from other tasks the owner runs in this folder (not part of the app) — never commit
scripts/jobs/
